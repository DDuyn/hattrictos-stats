{
  "name": "hattrictos-stats-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate"
  },
  "dependencies": {
    "@hattrictos-stats/shared": "workspace:*",
    "@libsql/client": "^0.17.0",
    "drizzle-orm": "^0.38.0",
    "fast-xml-parser": "^5.5.9",
    "hono": "^4.6.0",
    "oauth-1.0a": "^2.2.6",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0"
  }
}
